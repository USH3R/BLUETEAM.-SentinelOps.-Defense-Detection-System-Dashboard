services:
  sentinelops:
    build: .
    image: sentinelops:latest
    container_name: sentinelops_container
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    network_mode: "host"
    restart: "no"services:
  sentinelops:
    build: .
    image: sentinelops:latest
    container_name: sentinelops_container
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    network_mode: "host"
    restart: "no"services:
  sentinelops:
    build: .
    image: sentinelops:latest
    container_name: sentinelops_container
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    network_mode: "host"
    restart: "no"
